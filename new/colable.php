<?php
// made a re
function multiply($n, $factor = 2) {
    return $n * $factor;
}

echo multiply(5); // 10 (uses default 2)
echo multiply(5, 3); // 15 (overrides default)
// i just made a calculator well just basic 
